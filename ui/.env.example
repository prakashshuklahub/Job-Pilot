# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...   (server only — never expose to browser)
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...  (optional — for future client reads)

# Vercel Cron auth (Vercel sets this automatically on Pro; set manually for local cron tests)
# CRON_SECRET=generate-a-long-random-string
